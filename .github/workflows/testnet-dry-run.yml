name: Testnet Dry Run Validation

on:
  pull_request:
    paths:
      - 'devx/testnet_dry_run.py'
      - 'devx/test_testnet_dry_run.py'
      - 'cli/src/dry-run.ts'
      - 'cli/test/dry-run.test.ts'
      - '.github/workflows/testnet-dry-run.yml'
  push:
    branches:
      - main
      - develop

jobs:
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest
      
      - name: Run Python tests
        run: |
          cd devx
          python -m pytest test_testnet_dry_run.py -v

  typescript-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: |
          cd cli
          npm install
      
      - name: Run TypeScript tests
        run: |
          cd cli
          npm test -- --testPathPattern=dry-run.test.ts

  lint-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install flake8
        run: pip install flake8
      
      - name: Run flake8 on Python files
        run: |
          flake8 devx/testnet_dry_run.py devx/test_testnet_dry_run.py --max-line-length=100

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: |
          cd cli
          npm install
      
      - name: Run ESLint
        run: |
          cd cli
          npm run lint -- --ext .ts src/dry-run.ts

  dry-run-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Run Python dry run with valid input
        run: |
          echo '{
            "metadata": {
              "version": "v1",
              "timestamp": "2024-01-01T00:00:00Z",
              "hash": "abc123"
            },
            "payload": "test payload",
            "dependencies": []
          }' | python devx/testnet_dry_run.py
      
      - name: Run Python dry run with invalid input
        run: |
          echo '{
            "metadata": "invalid",
            "payload": "test payload"
          }' | python devx/testnet_dry_run.py || true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install CLI dependencies
        run: |
          cd cli
          npm install
      
      - name: Run TypeScript dry run with valid input
        run: |
          cd cli
          npx ts-node src/dry-run.ts --payload "test payload" --version v1 --timestamp "2024-01-01T00:00:00Z" --hash "abc123"
      
      - name: Run TypeScript dry run with invalid input
        run: |
          cd cli
          npx ts-node src/dry-run.ts --payload "test payload" --version v3 --timestamp "2024-01-01T00:00:00Z" --hash "abc123" || true